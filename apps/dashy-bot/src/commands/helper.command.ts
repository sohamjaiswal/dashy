import { CommandFunc } from './command.types';
export const enableHelper: CommandFunc = (client, message, args) => {
    return;
};
export const disableHelper: CommandFunc = (client, message, args) => {
    return;
};
