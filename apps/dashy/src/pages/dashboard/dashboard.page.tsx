import { Typography } from '@dashy/dashy-components';
import React from 'react';

const DashboardPage = () => {
    return (
        <div>
            <Typography label="Dashboard" weight={900} size={5} />
        </div>
    );
};

export default DashboardPage;
